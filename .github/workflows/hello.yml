name: Hello CI/CD Workflow

on:
  push:
    branches:
      - main


jobs:
  say_hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run hello script
        run: |
          chmod +x hello.sh
          ./hello.sh